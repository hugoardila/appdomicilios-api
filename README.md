# App Domicilios API

API PHP para pedidos, repartidores, notificaciones y recargas. Usa `.env.example` como plantilla y configura las credenciales de Firebase y ePayco fuera del control de versiones.

Los directorios de cargas, logs y datos de producción están excluidos.
